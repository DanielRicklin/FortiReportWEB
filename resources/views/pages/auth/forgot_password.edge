@layout()

    @slot('meta')
        <meta name="description" content="Forgot password page">
    @endslot

    <section class="bg-white dark:bg-gray-900">
        <div class="container flex items-center justify-center min-h-screen px-6 mx-auto">
            <form action="" method="post" class="w-full max-w-md">
                {{ csrfField() }}

                <h1 class="mt-3 text-2xl font-semibold text-gray-800 sm:text-3xl dark:text-white text-center">{{t('auth.FORGOT_PASSWORD')}}</h1>

                <div class="mt-8"></div>
                @!auth.input({ name : 'email', type : 'email'})

                <div class="mt-6">
                    <button type="submit" class="w-full px-6 py-3 text-sm font-medium tracking-wide text-white transition-colors duration-300 transform bg-blue-500 rounded-lg hover:bg-blue-400 focus:outline-none focus:ring focus:ring-blue-300 focus:ring-opacity-50">
                        {{t('auth.FORGOT_PASSWORD')}}
                    </button>
                </div>
            </form>
        </div>
    </section>
@end